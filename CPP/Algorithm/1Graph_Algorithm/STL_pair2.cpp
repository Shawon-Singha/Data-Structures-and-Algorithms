#include<bits/stdc++.h>
using namespace std;

int main(){
    pair<int,int> p;
    p = make_pair(10,20);
    cout << p.first << " " << p.second << endl;

    pair<string,int> q;
    q = make_pair("Shawon",20);
    cout << q.first << " " << q.second << endl;

    return 0;
}